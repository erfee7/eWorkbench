// src/common/auth/passwordPolicy.ts

/**
 * Keep this aligned with the CLI/user management scripts policy.
 * Reference: scripts/lib/validate.js (PASSWORD_MIN_LEN)
 */
export const PASSWORD_MIN_LEN = 6;